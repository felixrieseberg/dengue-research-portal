import toLetters from 'dengue/utils/to-letters';
import { module, test } from 'qunit';

module('Unit | Utility | to letters');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toLetters();
  assert.ok(result);
});
